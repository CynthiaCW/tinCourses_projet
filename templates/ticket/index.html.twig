{% extends 'base.html.twig' %}

{% block title %}Hello TickerController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {% if articles | length > 0 %}
        <table class="uk-table uk-table-middle uk-table-divider">
            <thead>
            <tr>
                <th class="uk-width-small">Nom article</th>
                <th>Description</th>
                <th>Image</th>
                <th>Quantité</th>
                <th>Modifier la quantité</th>
            </tr>
            </thead>
            <tbody>
            {% for article in articles %}
                <tr>
                    <td> {{ article.nom}} </td>
                    <td> {{ tincourses.article.description }}</td>
                    <td> {{ tincourses.article.image }}</td>
                    <td> {{ ticket.quantite }}</td>
                    <td><button class="uk-button uk-button-default" type="button" href="">+</button></td>
                    <td><button class="uk-button uk-button-default" type="button" href="">-</button></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p> Votre liste est vide</p>
    {% endif %}


    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/Coraline/Documents/DWWB CCI 2021-2022/symfony/tinCourses_projet/src/Controller/TicketController.php'|file_link(0) }}">src/Controller/TickerController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/Coraline/Documents/DWWB CCI 2021-2022/symfony/tinCourses_projet/templates/ticket/index.html.twig'|file_link(0) }}">templates/ticker/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
